---
status: completed
id: S2.9
title: Extractor Unit Tests
hours: 8
files:
  - tests/test_extractor.py
dependencies:
  - S2.1
  - S2.2
  - S2.3
  - S2.4
  - S2.5
  - S2.6
started: 2026-06-25
completed: 2026-06-25
---

# S2.9 — Extractor Unit Tests

## Description

Write comprehensive unit tests for the extraction layer covering:
full extraction, incremental extraction, upsert logic, error handling,
and edge cases.

## Acceptance Criteria

- [x] `test_full_extraction` — extracts all issues for a project
- [x] `test_incremental_extraction` — extracts only updated since last run
- [x] `test_flush_batch_persists_issues` — new issue inserted into DB
- [x] `test_transform_different_values` — existing issue fields updated
- [x] `test_empty_project_no_crash` — no issues, no crash
- [x] `test_changelog_parsed` — transitions parsed correctly
- [x] `test_extracts_sprint_ids` — sprint values extracted from customfield
- [x] `test_no_issues_returns_zero_counts` — no issues = zero stats
- [x] `test_missing_fields_does_not_crash` — partial data handled gracefully
- [x] `test_extraction_processes_all_projects` — multiple projects all processed
- [ ] Mock Jira API responses with `httpx` and `respx`

## Implementation Notes

- Use `respx` or `httpx_mock` for HTTP mocking
- Fixtures for mock Jira responses (issues, fields, changelogs)

## Files Created

- `tests/test_extractor.py` — NEW
