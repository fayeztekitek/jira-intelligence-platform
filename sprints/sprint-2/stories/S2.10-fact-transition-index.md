---
status: planned
id: S2.10
title: FactTransition Missing Index
hours: 1
files:
  - migrations/versions/002_add_transition_index.py
dependencies:
  - S2.2
started: null
completed: null
---

# S2.10 — FactTransition Missing Index

## Description

Add a database index on `fact_transition.changed_at` to speed up
time-in-status queries used by cycle-time computation and status
analytics.

## Acceptance Criteria

- [ ] Migration 002 adds `ix_fact_transition_changed_at` index
- [ ] `alembic upgrade head` applies index without errors
- [ ] `alembic downgrade -1` drops index cleanly

## Implementation Notes

- `CREATE INDEX ix_fact_transition_changed_at ON fact_transition (changed_at)`
- Also consider composite index: `(issue_id, changed_at)` for join queries

## Files Created

- `migrations/versions/002_add_transition_index.py` — NEW
