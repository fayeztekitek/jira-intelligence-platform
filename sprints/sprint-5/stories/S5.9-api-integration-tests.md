---
status: planned
id: S5.9
title: API Integration Tests
hours: 8
files:
  - tests/test_api.py
dependencies: []
started: null
completed: null
---

# S5.9 — API Integration Tests

## Description

Write comprehensive integration tests for all 15+ API endpoints using
seeded mock data in a test database.

## Acceptance Criteria

- [ ] `test_get_projects` — returns list of projects
- [ ] `test_get_kpi_delivery` — returns correct KPI values
- [ ] `test_get_kpi_quality` — returns quality metrics
- [ ] `test_get_kpi_risk` — returns risk control values
- [ ] `test_executive_summary` — returns full summary
- [ ] `test_get_risk_score` — returns risk with dimensions
- [ ] `test_get_issues` — paginated issue list
- [ ] `test_export_csv` — returns CSV with correct headers
- [ ] `test_export_xlsx` — returns valid Excel file
- [ ] `test_export_pdf` — returns valid PDF file
- [ ] `test_sprint_burndown` — returns daily data
- [ ] `test_kpi_history` — returns time series
- [ ] Coverage >= 80% across all API routes

## Implementation Notes

- Use `pytest` with `httpx.AsyncClient`
- Seed test database with known data
- Each test creates and cleans up its own data

## Files Changed

- `tests/test_api.py` — NEW comprehensive API tests
