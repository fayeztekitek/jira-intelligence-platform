---
status: completed
id: S1.7
title: Auth Unit Tests
hours: 4
files:
  - tests/test_auth.py
dependencies:
  - S1.2
started: 2026-06-25
completed: 2026-06-25
---

# S1.7 — Auth Unit Tests

## Description

Write unit tests for the authentication module covering token lifecycle,
API key validation, admin checks, and edge cases.

## Acceptance Criteria

- [x] `test_login_success` — valid API key returns JWT
- [x] `test_login_invalid_key` — invalid key returns 401
- [x] `test_token_expiry` — expired token returns 401
- [x] `test_require_auth_valid` — valid Bearer token passes
- [x] `test_require_auth_no_token` — missing header returns 401
- [x] `test_require_auth_invalid_signature` — tampered token returns 401
- [x] `test_require_admin_success` — admin token passes
- [x] `test_require_admin_forbidden` — non-admin token returns 403
- [x] `test_api_key_header` — X-API-Key header authenticates correctly

## Implementation Notes

- Use `pytest` with `httpx.AsyncClient` and FastAPI `TestClient`
- Mock `config.APP_SECRET_KEY` to a known value

## Files Created

- `tests/test_auth.py` — NEW
